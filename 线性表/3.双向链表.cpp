#include <stdio.h>
#include <stdlib.h>

#define MaxSize 20
typedef int ElemType;

//双向链表的结构体定义
struct DNOde {
    ElemType data;
    struct DNode *prior,*next;
}DNode,*DLinkLIst;

int main(void)
{
    return 0;
}